# Kinetix Backend API URL
# Set this in Vercel Project Settings > Environment Variables for production deployment
NEXT_PUBLIC_API_URL=https://backend-one-nu-39.vercel.app

# Groq API key for the Kinetix AI experiment assistant (server-side only — never expose to the client)
GROQ_API_KEY=
